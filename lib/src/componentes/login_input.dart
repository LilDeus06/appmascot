import 'package:flutter/material.dart';

class MyLoginInput extends StatelessWidget {
  const MyLoginInput({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}